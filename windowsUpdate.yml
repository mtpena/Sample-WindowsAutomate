---
- name: Install Windows Updates
  hosts: JUSTINE
  gather_facts: false
  tasks:
  - name: Install all security updates with automatic reboots
    win_updates:
      category_names:
      - SecurityUpdates
      reboot: yes
